import React from 'react';
import Navbar from '../Navbar/Navbar';

const Header = () => {
    return (
            <main className="row">
                <Navbar/>
            </main>     
    );
};

export default Header;